# Data Exporter – Factory Design Pattern

This project demonstrates the Factory Design Pattern by exporting the same dataset into
CSV, JSON, or XML formats based on user input at runtime.

## Features
- Runtime format selection
- Same dataset exported to different formats
- Loose coupling using Factory pattern

## Run
python main.py
